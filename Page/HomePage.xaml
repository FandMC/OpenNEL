<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenNEL_WinUI.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenNEL_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <local:StatusToVisibilityConverter x:Key="StatusToVisibilityConverter"/>
        <local:BoolNegationConverter x:Key="BoolNegationConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Announcement Section -->
        <Grid Grid.Row="0" Margin="0,0,0,10" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="公告" Style="{StaticResource SubtitleTextBlockStyle}" Margin="12,12,0,0"/>
            <ScrollViewer Grid.Row="1" Margin="12">
                <TextBlock Text="这里是公告内容..." TextWrapping="Wrap" />
            </ScrollViewer>
        </Grid>

        <!-- Account Section -->
        <Grid Grid.Row="1" Margin="0,10,0,0" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Margin="12,12,12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="账号" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="添加账号" Click="AddAccountButton_Click"/>
            </Grid>

            <Grid Grid.Row="1" Margin="12">
                <!-- Empty State -->
                <TextBlock Text="暂无账号" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="{x:Bind Accounts.Count, Converter={StaticResource CountToVisibilityConverter}, Mode=OneWay}"/>

                <!-- Account List -->
                <ListView ItemsSource="{x:Bind Accounts}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:AccountModel">
                            <Grid Padding="0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Account Info -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind EntityId}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="{x:Bind Channel}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>

                                <!-- Account Actions -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <!-- Login Button (Visible when offline) -->
                                    <Button Content="登录" 
                                            Click="LoginButton_Click" 
                                            Tag="{x:Bind}"
                                            Visibility="{x:Bind Status, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter='offline', Mode=OneWay}"
                                            IsEnabled="{x:Bind IsLoading, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}">
                                    </Button>
                                    
                                    <!-- Logged In Text (Visible when online) -->
                                    <TextBlock Text="已登录" 
                                               VerticalAlignment="Center" 
                                               Foreground="Green"
                                               Visibility="{x:Bind Status, Converter={StaticResource StatusToVisibilityConverter}, ConverterParameter='online', Mode=OneWay}"/>

                                    <!-- Delete Button -->
                                    <Button Content="删除" Click="DeleteAccountButton_Click" Tag="{x:Bind}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>
